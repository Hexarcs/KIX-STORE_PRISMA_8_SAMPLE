services:
  web:
    build: .
    container_name: kix_store_web
    ports:
      - "8003:8000"  # <- Mude apenas o primeiro número para 8003
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - DEBUG=1
    restart: unless-stopped
