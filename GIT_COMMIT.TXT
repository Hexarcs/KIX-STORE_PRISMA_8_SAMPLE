# 1. Adiciona todos os arquivos sanitizados
git add .

# 2. Faz o commit limpo
git commit -m "mensasagemcomit"

# 3. Envia para o repositório no GitHub
git push -u origin main

#verifica as mudanças

git status
git add .
git status

baixa do git
# 1. Veja a diferença exata feita no README pela web
git fetch

git diff HEAD origin/main

# 2. Traga a alteração para a sua máquina local
git pull
